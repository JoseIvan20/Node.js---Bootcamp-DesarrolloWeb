extends ./layout/mostrarPropiedad

block contenido 

    section(class="py-5")
        h2(class="text-center text-4xl font-extrabold pt-10 text-slate-400")= pagina

        if propiedades.length
            div(class="grid grid-cols-1 lg:grid-cols-3 gap-5 mt-5")
                each propiedad in propiedades 
                    include ./templates/card

        else 
            p(class="text-center font-bold text-slate-700 bg-slate-300 text-sm uppercase py-4 rounded shadow-sm mt-9") No hay resultados
